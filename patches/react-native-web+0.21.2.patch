diff --git a/node_modules/react-native-web/dist/cjs/exports/Modal/ModalPortal.js b/node_modules/react-native-web/dist/cjs/exports/Modal/ModalPortal.js
index f94b0aa..0d4d6b2 100644
--- a/node_modules/react-native-web/dist/cjs/exports/Modal/ModalPortal.js
+++ b/node_modules/react-native-web/dist/cjs/exports/Modal/ModalPortal.js
@@ -30,10 +30,10 @@ function ModalPortal(props) {
   React.useEffect(() => {
     if (_canUseDom.default) {
       return () => {
-        if (document.body && elementRef.current) {
+        if (document.body && elementRef.current && elementRef.current.parentNode === document.body) {
           document.body.removeChild(elementRef.current);
-          elementRef.current = null;
         }
+        elementRef.current = null;
       };
     }
   }, []);
diff --git a/node_modules/react-native-web/dist/cjs/exports/render/index.js b/node_modules/react-native-web/dist/cjs/exports/render/index.js
index 18d9b2f..dbe525e 100644
--- a/node_modules/react-native-web/dist/cjs/exports/render/index.js
+++ b/node_modules/react-native-web/dist/cjs/exports/render/index.js
@@ -19,9 +19,18 @@ function hydrate(element, root) {
   (0, _dom.createSheet)(root);
   return (0, _client.hydrateRoot)(root, element);
 }
+var _roots = typeof WeakMap !== 'undefined' ? new WeakMap() : null;
 function render(element, root) {
   (0, _dom.createSheet)(root);
-  var reactRoot = (0, _client.createRoot)(root);
+  // Reuse an existing root for this container instead of calling
+  // createRoot() again — Fast Refresh can re-run registerRootComponent on a
+  // full reload, and a second createRoot() over the same node produces two
+  // independent React roots fighting over the same DOM subtree.
+  var reactRoot = _roots && _roots.get(root);
+  if (!reactRoot) {
+    reactRoot = (0, _client.createRoot)(root);
+    if (_roots) _roots.set(root, reactRoot);
+  }
   reactRoot.render(element);
   return reactRoot;
 }
\ No newline at end of file
diff --git a/node_modules/react-native-web/dist/exports/Modal/ModalPortal.js b/node_modules/react-native-web/dist/exports/Modal/ModalPortal.js
index df3bcbb..1390eca 100644
--- a/node_modules/react-native-web/dist/exports/Modal/ModalPortal.js
+++ b/node_modules/react-native-web/dist/exports/Modal/ModalPortal.js
@@ -24,10 +24,10 @@ function ModalPortal(props) {
   React.useEffect(() => {
     if (canUseDOM) {
       return () => {
-        if (document.body && elementRef.current) {
+        if (document.body && elementRef.current && elementRef.current.parentNode === document.body) {
           document.body.removeChild(elementRef.current);
-          elementRef.current = null;
         }
+        elementRef.current = null;
       };
     }
   }, []);
diff --git a/node_modules/react-native-web/dist/exports/render/index.js b/node_modules/react-native-web/dist/exports/render/index.js
index 8f9a14d..85b1f33 100644
--- a/node_modules/react-native-web/dist/exports/render/index.js
+++ b/node_modules/react-native-web/dist/exports/render/index.js
@@ -15,9 +15,18 @@ export function hydrate(element, root) {
   createSheet(root);
   return domHydrateRoot(root, element);
 }
+var _roots = typeof WeakMap !== 'undefined' ? new WeakMap() : null;
 export default function render(element, root) {
   createSheet(root);
-  var reactRoot = domCreateRoot(root);
+  // Reuse an existing root for this container instead of calling
+  // createRoot() again — Fast Refresh can re-run registerRootComponent on a
+  // full reload, and a second createRoot() over the same node produces two
+  // independent React roots fighting over the same DOM subtree.
+  var reactRoot = _roots && _roots.get(root);
+  if (!reactRoot) {
+    reactRoot = domCreateRoot(root);
+    if (_roots) _roots.set(root, reactRoot);
+  }
   reactRoot.render(element);
   return reactRoot;
 }
\ No newline at end of file
