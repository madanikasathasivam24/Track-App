// Deferred until LiveMapScreen's real map/socket pass (see WHAT NOT TO BUILD YET).
import React from 'react';

interface RouteLineProps {
  coordinates: { latitude: number; longitude: number }[];
}

export function RouteLine(_props: RouteLineProps) {
  return null;
}
